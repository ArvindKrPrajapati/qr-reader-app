import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'fairwell-2018',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
